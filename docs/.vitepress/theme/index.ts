import DefaultTheme from 'vitepress/theme'
import './custom.css'

// Use the default VitePress theme with brand colors overridden in custom.css
// to match the Kubesonde frontend palette.
export default DefaultTheme
